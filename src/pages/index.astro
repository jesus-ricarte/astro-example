---
import Layout from '../layouts/Layout.astro';
import { LOCALES } from '../i18n/config';
---

<Layout>
	<main>
		<ul>
			{LOCALES.map((loc) => (
				<li>
					<a href={`/${loc.path}/`}>{loc.label}</a>
				</li>
			))}
		</ul>
	</main>
</Layout>
